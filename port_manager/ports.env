# Env file for variables shared between the bash and python port building layers
# that are used outside of the port building context (e.g. during initial port
# discovery).

PORTS_MIRROR_BASEURL="https://files.phoesys.com/ports/"
